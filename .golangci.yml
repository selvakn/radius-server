version: "2"

linters:
  default: none
  enable:
    - errcheck
    - staticcheck
    - govet
    - revive

  settings:
    revive:
      rules:
        - name: exported
          disabled: true

formatters:
  enable:
    - gofmt
