version: "2"

linters:
  default: none
  enable:
    - errcheck
    - staticcheck
    - govet
    - revive
    - gosec

  settings:
    revive:
      rules:
        - name: exported
          disabled: true

formatters:
  enable:
    - gofmt
